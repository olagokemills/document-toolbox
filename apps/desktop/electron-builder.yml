appId: com.privatepdf.desktop
productName: PrivatePDF
copyright: Copyright © 2024 PrivatePDF
directories:
  buildResources: build
  output: dist

files:
  - out/**/*
  - package.json

npmRebuild: false

mac:
  category: public.app-category.productivity
  artifactName: PrivatePDF.${ext}
  target:
    - target: dmg
      arch: [universal]
  darkModeSupport: true

win:
  artifactName: PrivatePDF-Setup.${ext}
  target:
    - target: nsis
      arch: [x64]

linux:
  target:
    - target: AppImage
      arch: [x64]

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
